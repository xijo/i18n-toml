# frozen_string_literal: true

RSpec.describe I18n::Toml do
  it "has a version number" do
    expect(I18n::Toml::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
